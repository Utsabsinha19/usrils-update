add in the project the file csv file upload option where the models will be coimpared and the suer can download the model and hyperparamters after the training. and the training paramerets will be showing live and teh user have to wait for the given timeline and the user can get through teh model training operations in that time and it will notify the user when it is done. also the user can give his/her own dataset for the training. in json file and it will train teh model based on that and show the results.what kind of differnce is being made by regular model training method and what are teh changes makinbg in the case of usrils what the projerct is about.